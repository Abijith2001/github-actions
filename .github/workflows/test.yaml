name: Test Workflow
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show working directory
        run: pwd

      - name: List files
        run: ls -la
